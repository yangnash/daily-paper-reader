---
title: Unleashing More Actions via Action Compositional Training for VLA Models
title_zh: 通过动作组合训练释放VLA模型的更多动作
authors: "Kai Peng, Jie Lu, Xiaojiang Peng"
date: 2026-07-01
pdf: "https://arxiv.org/pdf/2607.00351v1"
tags: ["query:zsc-action"]
score: 9.0
evidence: 提出动作组合训练方法以泛化到新的子技能组合
tldr: 视觉-语言-动作模型在机器人操作中常因过拟合特定行为模式而无法泛化到仅需子技能新组合的场景。为突破此瓶颈，提出ACT-VLA离线数据增强框架，利用模型潜在任务表示从现有任务自动合成物理可行的新演示。实验表明，基线模型泛化差，而用合成数据训练的策略在仿真任务中成功率显著提升。该方法无需额外人工数据收集即可扩展训练分布，为提升VLA泛化性提供了高效且可扩展的途径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.00351v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 893, \"height\": 486, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00351v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1871, \"height\": 1027, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00351v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 841, \"height\": 528, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.00351v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1512, \"height\": 318, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.00351v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 928, \"height\": 244, \"label\": \"Table\"}]"
motivation: VLA模型在机器人操作中过拟合，无法泛化到子技能新组合，且扩展高质量数据集成本高昂。
method: ACT-VLA框架通过离线数据增强，利用模型潜在任务表示从现有任务合成新演示，无需额外人工收集。
result: 在仿真操作任务中，用合成数据训练的模型成功率显著高于基线，验证了自动合成演示的有效性。
conclusion: 无需额外数据，自动扩展训练分布，可高效提升VLA模型泛化能力，降低数据成本。
---

## 摘要
视觉-语言-动作模型在机器人操控中表现出色，这得益于演示数据的规模和多样性。然而，标准训练范式常常导致VLA模型严重过拟合于特定的行为模式，即使在那些仅需相同子技能的崭新组合的场景中，也无法泛化到分布外情况。虽然扩展数据集可以缓解过拟合，但获取高质量的机器人数据仍然众所周知地劳动密集且成本高昂。为了在无需昂贵的人类远程操作的情况下解决这一僵局，并真正释放更多动作——即让VLA模型能够将已知子技能组合成远超原始演示的可执行行为——我们提出了ACT-VLA（VLA模型的行动组合训练），这是一种离线数据增强框架，利用模型的潜在任务表示，直接从现有任务中合成新颖、物理上有效的演示，用于策略训练。通过消除额外的手动数据收集，我们的方法自动扩展训练分布并减轻过拟合。我们在模拟中的具有挑战性的操控任务上评估了我们的方法。实验表明，基线VLA模型由于原始分布过拟合而泛化能力差，而使用我们合成数据训练的策略实现了显著更高的成功率，验证了利用现有任务进行自动演示合成为拓宽VLA泛化能力提供了一条有效、可扩展且数据高效的途径。

## Abstract
Vision-Language-Action models excel at robotic manipulation, driven by the scale and diversity of demonstration data. However, standard training paradigms often cause VLA models to severely overfit to specific behavioral patterns, rendering them unable to generalize to out-of-distribution scenarios even when those scenarios merely require novel combinations of identical sub-skills. While expanding datasets can mitigate this overfitting, acquiring high-quality robot data remains notoriously labor-intensive and cost-prohibitive. To resolve this impasse without expensive human teleoperation and to truly unleash more actions,i.e., enable VLA models to compose known sub-skills into a much broader set of executable behaviors beyond the original demonstrations-we propose ACT-VLA (Action Compositional Training for VLA Models), an offline data augmentation framework that leverages the model's latent task representations to synthesize novel, physically valid demonstrations directly from existing tasks for policy training. By eliminating additional manual data collection, our method automatically expands the training distribution and mitigates overfitting. We evaluate our approach on challenging manipulation tasks in simulation. Experiments demonstrate that while baseline VLA models generalize poorly due to original distribution overfitting, policies trained with our synthesized data achieve substantially higher success rates, validating that leveraging existing tasks for automated demonstration synthesis provides an effective, scalable, and data-efficient route to broadening VLA generalization.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：视觉-语言-动作（VLA）模型在机器人操控中表现优异，但标准训练范式导致模型严重过拟合于特定的行为模式。即使遇到仅需子技能全新组合的分布外场景，模型也无法泛化，因为训练数据中缺乏这些组合的演示。  
- **背景挑战**：扩展数据集可以缓解过拟合，但获取高质量机器人演示数据需要大量人工遥操作，成本高昂且难以覆盖组合爆炸式的任务空间。  
- **整体含义**：本文提出 ACT-VLA 框架，旨在无需额外人工数据收集的前提下，自动从现有任务中合成物理有效的新演示，从而扩大训练分布、释放模型组合已知子技能的能力，实现更广泛的泛化。

## 2. 论文提出的方法论

- **核心思想**：利用预训练 VLA 模型中蕴含的潜在组合结构（通过文本潜在插值 TLI），将推理时的隐式干预转变为离线数据生成引擎，合成新颖组合任务的轨迹，并将其作为训练数据增广现有数据集，使模型通过数据暴露内化组合性，不再依赖推理时的外部干预。  
- **关键技术细节**：  
  - **三阶段流程**：  
    1. **表示引导的轨迹合成**：使用 TLI 机制。给定两个基础任务的任务特定文本潜在表示 \(T_1, T_2\)，在每一步 \(i\) 通过线性插值修改文本隐藏状态：  
       \[
       h_T(i) \leftarrow h_T(i) + (1-\alpha)T_1 + \alpha T_2 - [(1-\alpha)T_2 + \alpha T_1]
       \]  
       其中 \(\alpha = i/\lambda\) 线性增长并截断至 \([0,1]\)，实现从任务 1 到任务 2 的平滑行为过渡。  
    2. **数据记录与处理**：记录成功 rollout 的多视角 RGB、机器人本体状态、执行动作，并构建与合成轨迹语义一致的自然语言指令（通过拆分重组基础任务指令）。  
    3. **增强策略训练**：将合成数据集与原始数据按套件级别等量混合，使用标准行为克隆训练 VLA 模型。  
  - **关键设计决策**：  
    - **质量过滤**：利用物理模拟器严格筛选，仅保留完整完成组合任务的轨迹，丢弃失败或多余步数的 rollout。  
    - **合成规模**：每个 OOD 套件合成约 300-400 条轨迹（每任务 30-40 条），与原始套件数据量持平。  
    - **训练动态**：保持原始与合成数据量在套件级平衡，防止合成分布压倒原始分布导致灾难性遗忘。  
- **算法流程文字说明**：  
  输入：预训练 VLA 模型 \(\pi_0\)、基础任务演示集；输出：合成数据集 \(D_{syn}\)。  
  1. 提取基础任务文本潜在表示 \(T_1, T_2\)；  
  2. 对每个组合任务，用 TLI 生成 rollout，经模拟器过滤；  
  3. 记录成功轨迹并构造指令；  
  4. 将 \(D_{syn}\) 加入原始数据，用行为克隆对 \(\pi_0\) 微调。

## 3. 实验设计

- **数据集/场景**：LIBERO 仿真基准，包含四个标准套件（Spatial, Object, Goal, LIBERO-10，各 10 个任务）和两个 OOD 套件（Spatial-OOD, Goal-OOD，各 10 个组合任务）。OOD 任务由基础任务中的物体与目标位置重配对构成，训练集从未见过。  
- **Benchmark**：LIBERO 基准套件（评估知识迁移与泛化）。  
- **对比方法**：  
  - 基线：UniVLA、OpenVLA-OFT、GR00T-N1、π0.5（均仅在原始 LIBERO 数据上训练）。  
  - 对照：π0 + 推理时 TLI（即 [12] 的方法）。  
  - 本文方法：基于 π0.5 backbone + ACT-VLA 合成数据训练。  
- **评估协议**：每个任务 3 个随机种子，每种子 50 个 episode，共 150 个 episode，报告平均成功率（%）。所有模型训练 30k 步，设置相同。

## 4. 资源与算力

- **明确信息**：论文仅提及 “All models are trained for 30k steps under identical settings.”，未说明 GPU 型号、数量、显存或训练时长。  
- **备注**：由于未明确披露算力细节，无法进行直接资源对比。通常此类工作使用单卡或少量 GPU（如 A100），但作者未报告。

## 5. 实验数量与充分性

- **主要实验**：  
  - 表 I：在 6 个套件上对比 5 个基线 + 本文方法，覆盖标准与 OOD 场景。  
  - 表 II：在 π0 backbone 上做消融，对比“仅原始数据”、“推理时 TLI”、“原始+合成数据”三种配置。  
- **实验数量**：共 2 张表，涵盖 7 种不同模型/配置，每个配置均按规范评估（每任务 150 episode）。  
- **充分性与公平性**：  
  - 消融控制了相同 backbone，比较了训练时与推理时合成方式，区分了数据来源的作用。  
  - 训练步数、评估协议统一，保证了公平对比。  
  - 但缺少对合成规模、不同 TLI 参数、多技能链（>2）的消融，也未在真实机器人上验证。总体而言，实验设计合理，但对于全面性仍有提升空间。

## 6. 论文的主要结论与发现

- 在 OOD 套件上，ACT-VLA 使 π0.5 的 Spatial-OOD 从 35.5% 提升至 88.2%（+52.7%），Goal-OOD 从 46.6% 提升至 95.6%（+49.0%）。  
- 六套件未加权平均 AVG 达 95.6%，超过所有基线（包括推理时 TLI 的 90.4%）。  
- 训练时合成（内化到权重）优于推理时潜在干预（需在线计算），且消除部署时额外开销。  
- 无需额外人工遥操作、无需更改架构、无需外部规划器，即可显著拓宽 VLA 的组合泛化能力。

## 7. 优点

- **数据高效**：零额外人工数据收集，自动合成物理有效的新组合演示。  
- **即插即用**：作为离线数据增强框架，可直接集成到现有 VLA 训练流程，无需修改模型架构。  
- **推理高效**：组合性内化于权重，部署时无额外计算（相比于推理时 TLI）。  
- **泛化能力强**：在 OOD 场景上大幅超越基线，且保持标准套件性能。  
- **设计细致**：包括指令自动构造、质量双过滤（任务完成+步数预算）、合成规模平衡，解决了“将 TLI 用于训练”的关键工程挑战。

## 8. 不足与局限

- **组合局限**：仅验证了两技能顺序组合（线性链），更长的多技能组合面临指数级优化挑战，论文未探索。  
- **依赖模型表示**：合成质量依赖预训练 VLA backbone 的语义解缠程度，弱基座模型可能成为瓶颈。  
- **仿真局限**：所有实验在仿真中完成，未在真实物理机器人上验证，存在 sim-to-real 差距。  
- **缺少算力报告**：未提供详细的训练资源消耗（GPU 型号、时长等），影响可复现性评估。  
- **消融深度有限**：缺少对合成规模、TLI 插值方式、不同过滤阈值的系统消融。

（完）
